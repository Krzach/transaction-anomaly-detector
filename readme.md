# Anomaly Detector

## Starting sequence

1. Run docker containers: *docker compose up -d*
2. Run Simulator and AnomalyDetector apps
3. Start a dashboard with: *streamlit run alarm_receiver/receiver.py*